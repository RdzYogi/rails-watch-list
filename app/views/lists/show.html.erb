<%= render partial: 'lists/nav' %>
<h2><%= @list.name %></h2>
<% @list.bookmarks.each do |b| %>
  <p><%= b.movie.title %></p>
  <p class="ps-2"><%= link_to "Remove", bookmark_path(b), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class:"text-decoration-none text-danger" %></p>
<% end %>
  <h2><%= link_to "Add a new movie", new_list_bookmark_path(@list), class: "btn btn-inverse text-secondary" %></h2>
